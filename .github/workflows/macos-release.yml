name: macOS Release

on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - '.github/workflows/macos-release.yml'
      - 'CMakeLists.txt'
      - 'cmake/**'
      - 'src/**'
      - 'dist/**'
      - 'data/**'
      - 'ext/**'
      - '3rdparty/**'
      - 'gst/**'
      - 'tests/**'
      - 'Brewfile'

permissions:
  contents: write

concurrency:
  group: macos-release-${{ github.ref }}
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  build-macos:
    name: Build macOS DMG
    runs-on: macos-15-intel
    timeout-minutes: 180

    env:
      HOMEBREW_NO_AUTO_UPDATE: '1'
      HOMEBREW_NO_INSTALL_CLEANUP: '1'
      HOMEBREW_NO_INSTALLED_DEPENDENTS_CHECK: '1'
      MACOSX_DEPLOYMENT_TARGET: '12.0'
      CMAKE_BUILD_PARALLEL_LEVEL: '2'
      MAKEFLAGS: '-j2'

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Ensure git tags are available
        run: |
          git fetch --force --tags origin
          git describe --tags --always

      - name: Configure git safe directory
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Install build dependencies
        run: |
          brew install \
            boost \
            chromaprint \
            cmake \
            fftw \
            gettext \
            glib \
            google-sparsehash \
            gst-libav \
            gst-plugins-bad \
            gst-plugins-base \
            gst-plugins-good \
            gst-plugins-ugly \
            gstreamer \
            libmtp \
            libplist \
            libxml2 \
            pkgconf \
            protobuf \
            qt@5 \
            sqlite \
            wget

      - name: Add Homebrew compatibility paths
        run: |
          set -euo pipefail
          BREW_PREFIX="$(brew --prefix)"
          QT5_PREFIX="$(brew --prefix qt@5)"
          GETTEXT_PREFIX="$(brew --prefix gettext)"
          LIBXML2_PREFIX="$(brew --prefix libxml2)"

          echo "BREW_PREFIX=$BREW_PREFIX" >> "$GITHUB_ENV"
          echo "QT5_PREFIX=$QT5_PREFIX" >> "$GITHUB_ENV"
          echo "GETTEXT_PREFIX=$GETTEXT_PREFIX" >> "$GITHUB_ENV"
          echo "LIBXML2_PREFIX=$LIBXML2_PREFIX" >> "$GITHUB_ENV"
          echo "$QT5_PREFIX/bin" >> "$GITHUB_PATH"
          echo "$GETTEXT_PREFIX/bin" >> "$GITHUB_PATH"

          if [[ "$BREW_PREFIX" == "/usr/local" && ! -e /usr/local/opt/qt5 ]]; then
            ln -s "$QT5_PREFIX" /usr/local/opt/qt5
          fi

      - name: Configure CMake
        working-directory: bin
        run: |
          set -euo pipefail

          BREW_PREFIX="${BREW_PREFIX:-$(brew --prefix)}"
          QT5_PREFIX="${QT5_PREFIX:-$(brew --prefix qt@5)}"
          GETTEXT_PREFIX="${GETTEXT_PREFIX:-$(brew --prefix gettext)}"
          LIBXML2_PREFIX="${LIBXML2_PREFIX:-$(brew --prefix libxml2)}"

          export PKG_CONFIG_PATH="${QT5_PREFIX}/lib/pkgconfig:${GETTEXT_PREFIX}/lib/pkgconfig:${LIBXML2_PREFIX}/lib/pkgconfig:${BREW_PREFIX}/lib/pkgconfig:${PKG_CONFIG_PATH:-}"
          export CMAKE_PREFIX_PATH="${QT5_PREFIX};${GETTEXT_PREFIX};${LIBXML2_PREFIX};${BREW_PREFIX}"
          export Qt5_DIR="${QT5_PREFIX}/lib/cmake/Qt5"
          export Qt5LinguistTools_DIR="${QT5_PREFIX}/lib/cmake/Qt5LinguistTools"
          export GST_SCANNER_PATH="${BREW_PREFIX}/opt/gstreamer/libexec/gstreamer-1.0/gst-plugin-scanner"
          export GST_PLUGIN_PATH="${BREW_PREFIX}/lib/gstreamer-1.0"

          echo "BREW_PREFIX=$BREW_PREFIX"
          echo "QT5_PREFIX=$QT5_PREFIX"
          echo "GETTEXT_PREFIX=$GETTEXT_PREFIX"
          echo "LIBXML2_PREFIX=$LIBXML2_PREFIX"
          echo "PKG_CONFIG_PATH=$PKG_CONFIG_PATH"
          echo "CMAKE_PREFIX_PATH=$CMAKE_PREFIX_PATH"
          echo "Qt5_DIR=$Qt5_DIR"
          echo "Qt5LinguistTools_DIR=$Qt5LinguistTools_DIR"

          cmake .. \
            -Wno-dev \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_OSX_ARCHITECTURES=x86_64 \
            -DGETTEXT_MSGMERGE_EXECUTABLE="${GETTEXT_PREFIX}/bin/msgmerge" \
            -DGETTEXT_MSGFMT_EXECUTABLE="${GETTEXT_PREFIX}/bin/msgfmt" \
            -DGETTEXT_XGETTEXT_EXECUTABLE="${GETTEXT_PREFIX}/bin/xgettext" \
            -DENABLE_AUDIOCD=OFF \
            -DENABLE_BREAKPAD=OFF \
            -DENABLE_BOX=OFF \
            -DENABLE_DROPBOX=OFF \
            -DENABLE_GOOGLE_DRIVE=OFF \
            -DENABLE_LIBGPOD=OFF \
            -DENABLE_LIBLASTFM=OFF \
            -DENABLE_LIBMTP=OFF \
            -DENABLE_MOODBAR=OFF \
            -DENABLE_SEAFILE=OFF \
            -DENABLE_SKYDRIVE=OFF \
            -DENABLE_SPARKLE=OFF \
            -DENABLE_SPOTIFY_BLOB=OFF \
            -DENABLE_VISUALISATIONS=OFF \
            -DENABLE_WIIMOTEDEV=OFF

      - name: Build Clementine
        working-directory: bin
        run: cmake --build . --parallel 2

      - name: Copy app resources
        working-directory: bin
        run: cmake --build . --target install --parallel 2

      - name: Ad-hoc sign app bundle
        working-directory: bin
        run: |
          codesign --force --deep --sign - clementine.app || true

      - name: Build DMG
        working-directory: bin
        run: cmake --build . --target dmg --parallel 2

      - name: Normalize artifact name
        id: package
        run: |
          set -euo pipefail
          SHORT_SHA="${GITHUB_SHA::7}"
          DESCRIBE="$(git describe --tags --always --dirty 2>/dev/null || echo "$SHORT_SHA")"
          SAFE_DESCRIBE="$(echo "$DESCRIBE" | tr '/ ' '--')"
          RELEASE_TAG="macos-${SAFE_DESCRIBE}-${GITHUB_RUN_NUMBER}"
          RELEASE_NAME="Clementine macOS ${DESCRIBE}"
          DMG="$(find bin -maxdepth 1 -name 'clementine-*.dmg' -print -quit)"

          if [[ -z "$DMG" ]]; then
            echo "No DMG was produced" >&2
            exit 1
          fi

          OUT="Clementine-macOS-x86_64-${SAFE_DESCRIBE}-${SHORT_SHA}.dmg"
          mkdir -p release
          cp "$DMG" "release/$OUT"

          echo "release_tag=$RELEASE_TAG" >> "$GITHUB_OUTPUT"
          echo "release_name=$RELEASE_NAME" >> "$GITHUB_OUTPUT"
          echo "artifact_name=$OUT" >> "$GITHUB_OUTPUT"

      - name: Upload DMG artifact
        uses: actions/upload-artifact@v4
        with:
          name: clementine-macos-dmg
          path: release/*.dmg
          if-no-files-found: error

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.package.outputs.release_tag }}
          name: ${{ steps.package.outputs.release_name }}
          body: |
            Automated macOS x86_64 build from `${{ github.sha }}`.

            Notes:
            - This build is ad-hoc signed, not Apple Developer ID notarized.
            - macOS Gatekeeper may require right-click → Open on first launch.
            - Some optional Clementine components are disabled to make the CI build reproducible with current Homebrew packages.
          files: release/*.dmg
          fail_on_unmatched_files: true
          draft: false
          prerelease: true
